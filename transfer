CREATE COMPUTE MODULE flow1_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		CALL CopyEntireMessage();
		RETURN TRUE;
	END;



	CREATE PROCEDURE CopyEntireMessage() BEGIN

		DECLARE BrokerName CHARACTER;
		DECLARE BrokerUserId CHARACTER;
		DECLARE BrokerVersion CHARACTER;
		DECLARE BrokerDataSource CHARACTER;

		executionGroupLabel
		QueueManagerName
		NodeLabel
		MessageFlowLabel
		ApplicationLabel
		LibraryLabel
		ExecutionGroupName
		Family
		ProcessId (Integer)
		QueueManagerName (Character)
		WorkPath (Character)

		Flow properties

		AdditionalInstances (Integer)
		CommitCount (Integer)
		CommitInterval (Integer)
		CoordinatedTransaction (Boolean)
		MessageFlowLabel (Character)
		ApplicationLabel (Character)
		LibraryLabel (Character)
		MaximumRateMsgsPerSec (Integer)
		NotificationThresholdMsgsPerSec (Integer)
		WlmPolicy (Character)

		Table 4. Node properties

		DataSource (Character)
		DataSourceUserId (Character)
		MessageOptions (Integer 64-bit)1
		NodeLabel (Character)
		ThrowExceptionOnDatabaseError (Boolean)1
		TransactionType (Character)1
		TreatWarningsAsErrors (Boolean)1

		SET BrokerName = BrokerName;
		SET BrokerUserId = BrokerUserId;
		SET BrokerVersion = BrokerVersion;
		SET BrokerDataSource = BrokerDataSource;		


		SET OutputRoot.JSON.Data.Brkname='BrokerName';

		SET OutputRoot.JSON.Data.BrokerVersion=BrokerVersion;

	END;
END MODULE;

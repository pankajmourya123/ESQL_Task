
CREATE COMPUTE MODULE flowtp_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		CALL CopyEntireMessage();
		RETURN TRUE;
	END;

	CREATE PROCEDURE CopyEntireMessage()
	BEGIN
		/* --set OutputRoot= InputRoot;
		SET OutputRoot.blob.blob.text=InputRoot.blob.blob;
		SET OutputRoot.JSON.Data.text=InputRoot.blob.blob LIKE '[0-9]%';
		-- REPLACE(InputRoot.JSON.Data, 'A', 'AA')
		-- RESULT = AABCDAABCDAABCDAA*/
		/*DECLARE myvar CHARACTER;
		DECLARE myblob BLOB;
		DECLARE parseData CHARACTER;
		SET myblob = InputRoot.BLOB.BLOB;
		SET parseData = CAST(myblob AS CHARACTER CCSID 1208);
		SET myvar = parseData;
		SET parseData=REPLACE(parseData, '908', '');
		SET OutputRoot.XMLNS.root=REPLACE(parseData, '908', '');*/

		

		 --SET blobstr = TRIM(LEADING '***' FROM blobstr);
		/*SET blobstr = TRIM(LEADING '1' FROM blobstr);
		SET blobstr = TRIM(LEADING '2' FROM blobstr);
		SET blobstr = TRIM(LEADING '3' FROM blobstr);
		SET blobstr = TRIM(LEADING '4' FROM blobstr);
		SET blobstr = TRIM(LEADING '5' FROM blobstr);
		SET blobstr = TRIM(LEADING '6' FROM blobstr);
		SET blobstr = TRIM(LEADING '7' FROM blobstr);
		SET blobstr = TRIM(LEADING '8' FROM blobstr);
		SET blobstr = TRIM(LEADING '9' FROM blobstr);*/


DECLARE blobstr CHARACTER;
		DECLARE temp CHARACTER;
		DECLARE blobstr2 BLOB;
		SET blobstr = CAST(InputRoot.BLOB.BLOB AS CHARACTER CCSID 1208);
		SET temp=LEFT(blobstr,3);
		SET blobstr = TRIM(LEADING temp FROM blobstr);
		CREATE LASTCHILD OF OutputRoot DOMAIN('XMLNS') PARSE(blobstr,
		InputProperties.Encoding,
		InputProperties.CodedCharSetId);
		
		
		
	
			
		



	END;

END MODULE;
